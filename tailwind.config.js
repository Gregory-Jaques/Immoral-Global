/** @type {import('tailwindcss').Config} */
module.exports = {
    content: ["*"],
    theme: {
        extend: {
            fontFamily: {
                'Lexend': ['Lexend', 'system-ui', 'sans-serif'],
            }
        }
    },
    plugins: [],
}